SQL Interview Questions (50) – Data Engineer (2–3 Years)
1. SQL Basics (1–5)

What is SQL? Explain different types of SQL commands (DDL, DML, DCL, TCL).

What is the difference between WHERE and HAVING?

What is the difference between DELETE, TRUNCATE, and DROP?

What are constraints in SQL? Explain primary key, foreign key, unique, and not null.

What is normalization? Explain different normal forms.

2. Joins (6–10)

Explain different types of joins: INNER, LEFT, RIGHT, FULL OUTER.

What is the difference between INNER JOIN and LEFT JOIN?

What is a self join? Give a real-world use case.

What is a cross join and when should it be used?

How do you find unmatched records between two tables?

3. Aggregation & Grouping (11–15)

What is GROUP BY? How does it work?

Difference between COUNT(*), COUNT(column), and COUNT(DISTINCT column).

How do you filter aggregated results?

What are aggregate functions? Explain common ones.

How do you find duplicate records in a table?

4. Subqueries & CTEs (16–20)

What is a subquery? Types of subqueries.

Difference between correlated and non-correlated subqueries.

What is a CTE (Common Table Expression)?

When should you use a CTE instead of a subquery?

What is a recursive CTE? Give a use case.

5. Window Functions (21–30)

What is a window function?

Difference between GROUP BY and window functions.

Explain ROW_NUMBER().

Difference between ROW_NUMBER(), RANK(), and DENSE_RANK().

How do you find the second highest salary using window functions?

What is the use of PARTITION BY?

What is ORDER BY inside window functions?

Explain running total using window functions.

How do you find top N records per group?

Explain LAG() and LEAD() with use cases.

6. Data Manipulation & Transformation (31–35)

How do you remove duplicate records from a table?

How do you pivot and unpivot data?

How do you handle NULL values in SQL?

What is CASE WHEN and where is it used?

How do you merge data from two tables (UPSERT logic)?

7. Performance Optimization (36–42)

What is indexing and how does it improve performance?

Difference between clustered and non-clustered index.

What is query execution plan?

How do you optimize a slow SQL query?

What are common causes of poor SQL performance?

When should you avoid using indexes?

What is partitioning in databases?

8. Transactions & ACID (43–46)

What is a transaction in SQL?

Explain ACID properties.

What is the difference between COMMIT and ROLLBACK?

What are isolation levels?

9. Data Engineering Scenarios (47–50)

How do you implement incremental data loading using SQL?

How do you identify changed records between two datasets?

How do you handle Slowly Changing Dimensions (SCD Type 1 and Type 2)?

How do you write SQL to validate data quality (duplicates, null checks, row counts)?
